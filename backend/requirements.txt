# ---- Stack definido en el documento ----
fastapi==0.115.2          # Backend / API REST
pydantic==2.13.5          # Validacion de esquemas (viene con FastAPI). >=2.11.7 exigido por supabase 2.31.0
supabase==2.31.0          # Base de datos: PostgreSQL gestionado (Supabase). 2.9.1 no soporta las API keys nuevas (sb_secret_/sb_publishable_)
PyJWT==2.14.0             # JWT propio firmado por el backend (RNF1). >=2.12.0 exigido por supabase 2.31.0
bcrypt==4.2.0             # Cifrado (hash) de contrasenas (RNF1)

# ---- Runtime minimo para poder ejecutar el stack ----
uvicorn[standard]==0.31.0 # Servidor ASGI que ejecuta FastAPI
python-multipart==0.0.12  # Necesario para leer formularios en FastAPI
python-dotenv==1.0.1      # Cargar variables de entorno en local

# ---- Pruebas ----
pytest==8.3.3
httpx==0.27.2              # Requerido por fastapi.testclient.TestClient
